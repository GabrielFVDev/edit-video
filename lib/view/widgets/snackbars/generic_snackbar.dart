import 'package:flutter/material.dart';

class GenericSnackbar extends StatelessWidget {
  const GenericSnackbar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
